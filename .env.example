# Deployment Configuration
NODE_ENV=production
SERVE_FRONTEND=true

# Note: This file is for deployment configuration only.
# Backend-specific environment variables should be in backend/.env
# Frontend-specific environment variables should be in frontend/.env
